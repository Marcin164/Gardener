import React, { useEffect } from "react";
import { getData } from "../api/websocket";

type Props = {};

const Dashboard = (props: Props) => {
  return <div>Dashboard</div>;
};

export default Dashboard;
